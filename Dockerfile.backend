FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir fastapi uvicorn huggingface_hub transformers torch requests python-dotenv

COPY api_backend.py .

EXPOSE 8080
CMD ["sh", "-c", "uvicorn api_backend:app --host 0.0.0.0 --port ${PORT:-8080}"]
